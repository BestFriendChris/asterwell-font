"""Build pipeline for the Asterwell Text font family.

The package is a private build tool, not a library: it is installed into the
project's own virtualenv so that ``asterwell-build`` is on PATH, and every
entry point is reached through :mod:`asterwell_build.cli`.
"""
